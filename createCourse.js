import { people } from "./video";


export function createCourse(input) {
    const courseExists = people.find((class { })); {
        return class { }.courseName === input.coursename;
    };
    const newCourse = {
        courseName: input.courseName,
    };
    people.push(newCourse);
}

